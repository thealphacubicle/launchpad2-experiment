# Install the package in editable mode
-e .

# Core dependencies
langchain
langchain-community
langchain-openai
streamlit
python-dotenv
streamlit-mermaid

# MCP and HTTP dependencies
mcp>=1.2
httpx>=0.27

# Development dependencies
pre-commit>=3.6
ruff>=0.5
